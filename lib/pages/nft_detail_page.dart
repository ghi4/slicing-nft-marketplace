import 'package:flutter/material.dart';

class NftDetailPage extends StatelessWidget {
  const NftDetailPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('NFT Detail Page'),
    );
  }
}
